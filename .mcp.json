{
  "name": "pet-care-ai-mcp",
  "description": "Pet Care Ai MCP server. Tools: generate feeding schedule, track vaccinations, identify breed. Built by MEOK AI Labs.",
  "version": "1.0.0",
  "tools": [
    {
      "name": "generate_feeding_schedule",
      "description": "Generate a tailored feeding schedule for a pet.",
      "parameters": {
        "type": "object",
        "properties": {
          "species": {
            "type": "string"
          },
          "breed": {
            "type": "string"
          },
          "weight_kg": {
            "type": "number"
          },
          "age_months": {
            "type": "integer"
          },
          "activity_level": {
            "type": "string"
          },
          "special_needs": {
            "type": "array"
          }
        },
        "required": [
          "species"
        ]
      }
    },
    {
      "name": "track_vaccinations",
      "description": "Track vaccination status and generate upcoming schedule.",
      "parameters": {
        "type": "object",
        "properties": {
          "species": {
            "type": "string"
          },
          "age_weeks": {
            "type": "integer"
          },
          "vaccinations_given": {
            "type": "array"
          },
          "indoor_only": {
            "type": "boolean"
          }
        },
        "required": [
          "species",
          "age_weeks"
        ]
      }
    },
    {
      "name": "identify_breed",
      "description": "Identify likely breed from physical and behavioral characteristics.",
      "parameters": {
        "type": "object",
        "properties": {
          "species": {
            "type": "string"
          },
          "characteristics": {
            "type": "object"
          }
        },
        "required": [
          "species",
          "characteristics"
        ]
      }
    },
    {
      "name": "check_health_symptoms",
      "description": "Check pet health symptoms and get guidance on urgency and next steps.",
      "parameters": {
        "type": "object",
        "properties": {
          "species": {
            "type": "string"
          },
          "symptoms": {
            "type": "array"
          },
          "age_months": {
            "type": "integer"
          },
          "additional_info": {
            "type": "string"
          }
        },
        "required": [
          "species",
          "symptoms"
        ]
      }
    },
    {
      "name": "get_training_recommendations",
      "description": "Get personalized training recommendations for a pet.",
      "parameters": {
        "type": "object",
        "properties": {
          "species": {
            "type": "string"
          },
          "breed": {
            "type": "string"
          },
          "age_months": {
            "type": "integer"
          },
          "issues": {
            "type": "array"
          },
          "experience_level": {
            "type": "string"
          }
        },
        "required": [
          "species"
        ]
      }
    }
  ]
}